<UserControl x:Class="WPFCore.ElectGrid.LV.UListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wl="clr-namespace:WPFCore.ElectGrid.LV"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel LastChildFill="True">
        <Label  DockPanel.Dock="Top" Content="{Binding Path=Label}"></Label>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition MinWidth="150" MaxWidth="250"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <ListView 
                Name="_lvwData" 
                Grid.Column="0"
                DataContext="{Binding Path=ListVM}"
                IsEnabled="{Binding Path=IsEnabled}"
                ItemsSource="{Binding Path=ListItemsView}">
            </ListView>
            <GridSplitter 
                Grid.Column="1"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Width="5">
            </GridSplitter>
            <wl:UListFilter
                Margin="5 0 0 0"
                Grid.Column="1"></wl:UListFilter>
        </Grid>
    </DockPanel>
</UserControl>
